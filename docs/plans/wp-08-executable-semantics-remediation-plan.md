# WP-08 Executable Semantics Remediation Plan

## Scope

- Baseline: `01dcca29064cf4c4d7fd2b1aa73f9e9f046943b7`
- Evidence: local static and synthetic local tests only
- Runtime: Node.js `22.23.1`
- Private projects, tenants, company systems, and publication: excluded

## Steps

1. Preserve RED probes for conditional early exits, disguised connector URIs,
   missing grant assignments, tautological HTTP classification, and
   parameter-only index plans.
2. Make the supported frontend recognizer conservative and fail closed.
3. Parse exact canonical SharePoint endpoint paths and query identities.
4. Require executable authorization, permission, HTTP, and index action chains
   with dominance, payload, response data flow, failure branches, and readback.
5. Keep each builder evidence section independently fail closed.
6. Run a compiled CLI process over raw synthetic source, definition, ZIP, and
   manifest files for the fourteen contract-required WP-06 rules.
7. Record local verification separately from every tenant and publication gate.

## Acceptance

- The five RED probes become GREEN without weakening their assertions.
- All six builder sections derive from the corrected synthetic definition.
- URI, action-lineage, and response-data-flow mutations remove only the affected
  trusted section.
- The global CLI scope remains the default; `--required-only` is explicit.
- Build, focused tests, full tests, diff check, and dependency audit pass on
  Node.js 22.
- The unavailable official public-data scanner remains `NOT_RUN`.

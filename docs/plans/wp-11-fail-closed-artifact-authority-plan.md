# WP-11 Fail-Closed Artifact Authority Plan

## Objective

Close the independent blockers at baseline
`da422100f320bd0019adabf8136a42d94c5cc0aa` without private source access,
tenant access, external mutation, or publication. Trusted facts must be derived
only from the complete executable synthetic artifact and exact contract.

## Sequence

1. Preserve top-level index, schema, permission, and protected-item bypass writes as compiled-CLI RED regressions.
2. Inspect every normalized action before emitting any builder section and require each mutating action to belong to the exact proven contract-derived action set.
3. Compare all supported field properties and require exact type-specific Choice, Lookup, and DateTime create payloads.
4. Replace raw OData fragment acceptance with one exact allowlisted-field equality grammar and escaped literal data.
5. Require Save readback to parse the GET response and compare every written field/value before success.
6. Preserve exact frontend inventory, definition, native ZIP, manifest, package digest, and all-fourteen-rule compiled CLI coverage.
7. Preserve catalog-derived rule-specific `LIVE_SMOKE NOT_RUN` gates.
8. Run Node 22 build, focused tests, full tests, diff check, dependency audit, official public-data scanner attempt, and supplemental public-safety checks.

## Claim Boundary

WP-11 can establish only local static and synthetic package-artifact evidence.
It cannot establish tenant discovery, Preflight, Apply, import, rebinding,
enablement, execution, mutation, live smoke, semantic or effective-permission
readback, tenant verification, publication, or publication readback.

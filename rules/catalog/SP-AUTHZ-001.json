{
  "schemaVersion": "1.0",
  "id": "SP-AUTHZ-001",
  "title": "Server authority re-read",
  "domain": "sharepoint",
  "severity": "error",
  "description": "Protected mutation is not preceded by server identity and active capability re-read.",
  "rationale": "Client actor and protected business values cannot authorize a server mutation.",
  "appliesTo": [{ "kind": "builder", "profile": "wp06-evidence-v1" }],
  "detector": {
    "implementation": "packages/rules/src/sharepoint/authorization.ts",
    "exportName": "spAuthz001",
    "input": "artifact-graph",
    "deterministic": true,
    "network": "forbidden",
    "parameters": {}
  },
  "red": {
    "root": "fixtures/rules/SP-AUTHZ-001/red",
    "expectedResult": "FAIL",
    "expectedDiagnosticCodes": ["SP-AUTHZ-001"]
  },
  "green": {
    "root": "fixtures/rules/SP-AUTHZ-001/green",
    "expectedResult": "PASS",
    "expectedDiagnosticCodes": []
  },
  "finalArtifact": { "required": true, "artifactKinds": ["generated-definition", "zip"] },
  "remediation": ["Re-read system identity, one active capability row, current protected state, and required operation before mutation."],
  "residualGate": {
    "required": true,
    "claimClass": "LIVE_SMOKE",
    "description": "A controlled tamper attempt and authenticated authority readback remain external."
  },
  "supportedEvidence": ["LOCAL_STATIC", "PACKAGE_ARTIFACT"],
  "tags": ["sharepoint", "authorization", "offline", "synthetic"]
}

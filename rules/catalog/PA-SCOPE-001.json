{
  "schemaVersion": "1.0",
  "id": "PA-SCOPE-001",
  "title": "Loop-safe termination scope",
  "domain": "power-automate",
  "severity": "error",
  "description": "Terminate action is nested within a loop scope.",
  "rationale": "The inspected final artifact must preserve the declared offline safety contract.",
  "appliesTo": [
    {
      "kind": "definition"
    },
    {
      "kind": "zip",
      "profile": "power-platform-solution-v1"
    }
  ],
  "detector": {
    "implementation": "packages/rules/src/power-automate/scope.ts",
    "exportName": "paScope001",
    "input": "artifact-graph",
    "deterministic": true,
    "network": "forbidden",
    "parameters": {}
  },
  "red": {
    "root": "fixtures/rules/PA-SCOPE-001/red",
    "expectedResult": "FAIL",
    "expectedDiagnosticCodes": [
      "PA-SCOPE-001"
    ]
  },
  "green": {
    "root": "fixtures/rules/PA-SCOPE-001/green",
    "expectedResult": "PASS",
    "expectedDiagnosticCodes": []
  },
  "finalArtifact": {
    "required": true,
    "artifactKinds": [
      "generated-definition",
      "zip"
    ]
  },
  "remediation": [
    "Move Terminate outside every loop container and route the loop result to an explicit terminal action after the loop completes."
  ],
  "residualGate": {
    "required": true,
    "claimClass": "LIVE_SMOKE",
    "description": "Import and controlled failure-path execution remain external."
  },
  "supportedEvidence": [
    "LOCAL_STATIC",
    "PACKAGE_ARTIFACT"
  ],
  "tags": [
    "power-automate",
    "offline",
    "synthetic"
  ]
}

{
  "schemaVersion": "1.0",
  "id": "PA-LIMIT-001",
  "title": "Flow action budget",
  "domain": "power-automate",
  "severity": "error",
  "description": "Flow action count exceeds its declared or platform budget.",
  "rationale": "The inspected final artifact must preserve the declared offline safety contract.",
  "appliesTo": [
    {
      "kind": "definition"
    },
    {
      "kind": "zip",
      "profile": "power-platform-solution-v1"
    }
  ],
  "detector": {
    "implementation": "packages/rules/src/power-automate/action-limit.ts",
    "exportName": "paLimit001",
    "input": "artifact-graph",
    "deterministic": true,
    "network": "forbidden",
    "parameters": {}
  },
  "red": {
    "root": "fixtures/rules/PA-LIMIT-001/red",
    "expectedResult": "FAIL",
    "expectedDiagnosticCodes": [
      "PA-LIMIT-001"
    ]
  },
  "green": {
    "root": "fixtures/rules/PA-LIMIT-001/green",
    "expectedResult": "PASS",
    "expectedDiagnosticCodes": []
  },
  "finalArtifact": {
    "required": true,
    "artifactKinds": [
      "generated-definition",
      "zip"
    ]
  },
  "remediation": [
    "Reduce the normalized action count or split the processor while keeping each flow within both its project contract budget and the profile limit."
  ],
  "residualGate": {
    "required": true,
    "claimClass": "IMPORTED",
    "description": "Platform import remains an external gate."
  },
  "supportedEvidence": [
    "LOCAL_STATIC",
    "PACKAGE_ARTIFACT"
  ],
  "tags": [
    "power-automate",
    "offline",
    "synthetic"
  ]
}

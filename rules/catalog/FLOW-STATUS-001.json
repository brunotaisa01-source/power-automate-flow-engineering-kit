{
  "schemaVersion": "1.0",
  "id": "FLOW-STATUS-001",
  "title": "Semantic readback before success",
  "domain": "power-automate",
  "severity": "error",
  "description": "Succeeded completion is reachable without successful semantic readback.",
  "rationale": "The inspected final artifact must preserve the declared offline safety contract.",
  "appliesTo": [
    {
      "kind": "definition"
    },
    {
      "kind": "zip",
      "profile": "power-platform-solution-v1"
    }
  ],
  "detector": {
    "implementation": "packages/rules/src/power-automate/status.ts",
    "exportName": "flowStatus001",
    "input": "artifact-graph",
    "deterministic": true,
    "network": "forbidden",
    "parameters": {}
  },
  "red": {
    "root": "fixtures/rules/FLOW-STATUS-001/red",
    "expectedResult": "FAIL",
    "expectedDiagnosticCodes": [
      "FLOW-STATUS-001"
    ]
  },
  "green": {
    "root": "fixtures/rules/FLOW-STATUS-001/green",
    "expectedResult": "PASS",
    "expectedDiagnosticCodes": []
  },
  "finalArtifact": {
    "required": true,
    "artifactKinds": [
      "generated-definition",
      "zip"
    ]
  },
  "remediation": [
    "Run the contract readback and semantic assertion after mutation, then allow Succeeded only through runAfter edges that require successful readback and assertion."
  ],
  "residualGate": {
    "required": true,
    "claimClass": "TENANT_VERIFIED",
    "description": "Controlled mutation and semantic effect verification remain external."
  },
  "supportedEvidence": [
    "LOCAL_STATIC",
    "PACKAGE_ARTIFACT"
  ],
  "tags": [
    "power-automate",
    "offline",
    "synthetic"
  ]
}

{
  "schemaVersion": "1.0",
  "id": "PA-CONNECTION-001",
  "title": "Explicit transitive connection references",
  "domain": "power-automate",
  "severity": "error",
  "description": "Flow connection references are missing, implicit, inconsistent, or unused.",
  "rationale": "The inspected final artifact must preserve the declared offline safety contract.",
  "appliesTo": [
    {
      "kind": "definition"
    },
    {
      "kind": "zip",
      "profile": "power-platform-solution-v1"
    }
  ],
  "detector": {
    "implementation": "packages/rules/src/power-automate/connection-reference.ts",
    "exportName": "paConnection001",
    "input": "artifact-graph",
    "deterministic": true,
    "network": "forbidden",
    "parameters": {}
  },
  "red": {
    "root": "fixtures/rules/PA-CONNECTION-001/red",
    "expectedResult": "FAIL",
    "expectedDiagnosticCodes": [
      "PA-CONNECTION-001"
    ]
  },
  "green": {
    "root": "fixtures/rules/PA-CONNECTION-001/green",
    "expectedResult": "PASS",
    "expectedDiagnosticCodes": []
  },
  "finalArtifact": {
    "required": true,
    "artifactKinds": [
      "generated-definition",
      "zip"
    ]
  },
  "remediation": [
    "Make normalized package references, connector references, and the flow contract reference set equal, explicit, and transitively used."
  ],
  "residualGate": {
    "required": true,
    "claimClass": "REBOUND",
    "description": "Authenticated connection rebind readback remains external."
  },
  "supportedEvidence": [
    "LOCAL_STATIC",
    "PACKAGE_ARTIFACT"
  ],
  "tags": [
    "power-automate",
    "offline",
    "synthetic"
  ]
}
